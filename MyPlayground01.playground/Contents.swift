import UIKit

/*
 คีย์เวิร์ด let ใช้กับค่าคงที่ ส่วน var ใช้กับตัวแปร
 ตัวแปร numberOfCounts มีค่าเป็น 10 กับคีย์เวิร์ด let หมายความว่าต้องการให้ค่านั้นไม่เปลี่ยนแปลง แต่ levelOfGame มีค่า 0 กับคีย์เวิร์ด var ซึ่งหมายความว่าค่านั้นอาจเปลี่ยนแปลง
 */
let numberOfCounts = 10
var levelOfGame = 0
