import UIKit

/*
 อธิบายตัวดําเนินการใน Swift

 1. Unary Operators
 - ดําเนินการกับค่าเดียวเท่านั้น เช่น ตัวดําเนินการ - หรือ ! จะวางไว้ด้านหน้าหรือหลังค่านั้นๆ
 - ตัวอย่างเช่น -a, !done

 2. Binary Operators
 - ดําเนินการกับค่าสองตัว เช่น a + b, score <= maxScore
 - วางไว้ระหว่างค่าทั้งสองนั้น

 3. Ternary Operator (เพียงตัวเดียว)
 - ดําเนินการกับค่าสามตัวคือ ? :
 - เช่น score > 80 ? "ผ่าน" : "ไม่ผ่าน"

 */
